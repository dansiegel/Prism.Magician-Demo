using Prism.Magician;

namespace MagicDemo.SampleModule.ViewModels
{
    [ViewModelBase]
    public abstract partial class ViewModelBase 
    {
        // The generated code will add BindableBase as the base type
        // We will also get a constructor with the generated BaseServices
    }
}
